[pytest]
markers =
    integration: mark test as integration requiring Ollama server
    searxng: mark test as requiring a running SearXNG instance
    e2e: mark test as end-to-end requiring Playwright and running services

addopts = -m "not e2e"
