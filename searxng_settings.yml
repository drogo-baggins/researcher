# SearXNG settings file for researcher project
# Minimal override — only what's needed for JSON API integration.
# All other settings inherit from the image's built-in defaults.

use_default_settings: true

search:
  formats:
    - html
    - json

server:
  port: 8080
  bind_address: "0.0.0.0"
  secret_key: "researcher-local-dev-a7f3b9c2e1d4"
  image_proxy: true
  method: "GET"
